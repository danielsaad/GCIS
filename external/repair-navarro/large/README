
a slower version to compress large sequences

it receives a second parameter <MB>, and does it best to perform as fast
as possible while not exceeding that space.

implemented for irepair and bal/ as well
